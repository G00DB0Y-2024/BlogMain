# 推荐系统资深算法工程师面试考卷

**说明**：本试卷主要考察候选人在推荐系统前沿召回技术、因果推断基础、自监督学习以及工程架构等方面的综合能力。请在下方空白处作答，要求逻辑清晰、详略得当。

---

## 第一部分：因果推断与偏差纠正 (Causal Inference)

**1. Rubin 因果模型与反事实推断**
在 Rubin 因果模型中，我们定义了 ITE、ATE 和 CATE。
请解释“因果推断的根本问题是反事实推断”这句话的含义。具体而言，在计算 ATE (Average Treatment Effect) 时，为什么期望符号内的两项都各自包含了一半的反事实数据？请结合总体样本的分配进行详细推导与说明。

<br><br><br><br><br><br><br><br>

**2. 可忽略的分配机制 (Ignorable Assignment Mechanism)**
要从存在选择偏差的观察性数据中识别因果效应，必须满足两大核心假设。
(1) 请写出这两个假设，并解释其业务/统计含义。
(2) 在实际工程中，如果发现某些特征组合下“倾向得分等于 0”（完全缺失某组数据），这破坏了哪个假设？在系统设计和数据处理上有哪些应对策略？

<br><br><br><br><br><br><br><br>

---

## 第二部分：双塔模型与自监督学习 (Two-Tower & SSL)

**3. 双塔模型的长尾困境**
双塔模型往往在长尾物品的表征学习上表现不佳。请从以下三个维度，深度剖析双塔模型面临的选择偏差与学习滞后问题：
- 正样本视角的比例失衡
- 批内负采样 (In-batch Negatives) 带来的问题
- 全局随机采样造成的马太效应

<br><br><br><br><br><br><br><br>

**4. 自监督对比学习 (Self-Supervised Learning)**
为了脱离对点击行为的单一依赖，我们在物品塔引入自监督学习（Data Augmentation）。
(1) 请描述自监督学习中“正负样本对”的构造逻辑以及损失函数（InfoNCE）的核心思想。
(2) 讲义中提到了四种特征增强方法（Random Mask, Dropout, Complementary, MI-based Masking）。请详细解释“关联特征掩蔽 (MI-based Masking)”，并说明为什么它能防止神经网络“偷懒抄近道”？

<br><br><br><br><br><br><br><br>

---

## 第三部分：Deep Retrieval 深度召回 (DR)

**5. 范式转移：DR 与 传统双塔+ANN**
Deep Retrieval 旨在颠覆传统的“双塔 + 向量数据库 (ANN)”链路。请从**检索空间**、**索引机制**和**匹配计算**三个维度，对比说明 DR 与传统双塔的本质区别，以及 DR 是如何消除“表征与索引脱节”的精度损失的？

<br><br><br><br><br><br><br><br>

**6. 线上召回与 Beam Search**
在 DR 的线上召回阶段，系统如何将用户映射为路径并最终取回物品？对于深度为 $D$、每层宽度为 $K$ 的网络，系统如何利用 Beam Search 剪枝机制控制计算复杂度？请简述其执行流程。

<br><br><br><br><br><br><br><br>

**7. 离线训练机制**
DR 的离线训练采用了交替优化的类 EM 机制。在固定神经网络、学习“物品表征”（即为物品分配路径）的步骤中：
为什么要在目标函数中引入“拥挤度惩罚项 $\mathcal{R}(	au)$”？如果不加这个惩罚项，系统会出现什么灾难性后果？

<br><br><br><br><br><br><br><br>

---

## 第四部分：召回策略与工程扩充 (Recall & Cache)

**8. 地理位置与作者召回的工程细节**
(1) **地理位置召回**：使用 GeoHash 检索时，为什么工程上必须采用“九宫格召回策略”？
(2) **作者召回**：在“关注作者召回 (U2A2I)”中，如果某个头部大V一天发布了50篇笔记，系统应如何处理以避免用户的 Feed 流被霸屏？请列举具体的工程限制措施。

<br><br><br><br><br><br><br><br>

**9. 缓存召回机制 (Cache Recall)**
精排层产生了大量高分候选，但最终被重排/打散策略淘汰，造成了算力浪费。为此我们设计了“缓存召回通道”。
为了避免缓存池被陈旧内容塞满，必须配合多维度的生命周期管理。请详细列出并解释四种核心的“退场机制 (Cache Eviction Policies)”。

<br><br><br><br><br><br><br><br>

---

## 第五部分：召回后处理与布隆过滤器 (Bloom Filter)

**10. 曝光过滤与 Bloom Filter 原理**
(1) 为什么 Bloom Filter 的查验结果是“一定没看过”和“可能看过”？请从哈希碰撞的角度解释“误伤 (False Positive)”是如何产生的。
(2) 假设业务可容忍的极限误伤概率为 $\delta$，曝光物品总数为 $n$。请写出最优哈希函数个数 $k$ 和最优二进制向量长度 $m$ 的工程配置公式。
(3) 业务痛点：为什么标准的 Bloom Filter 绝对不支持删除过期物品的操作？这会导致什么长期问题？

<br><br><br><br><br><br><br><br>
